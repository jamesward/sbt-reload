# Start the server
> runReload
$ sleep 2000
$ exists target/server-started.txt

# Simulate a restart (source didn't change but we can just re-invoke)
$ delete target/server-started.txt
> runReload
$ sleep 2000
$ exists target/server-started.txt
