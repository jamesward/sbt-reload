# Start the server
> runReload
> waitForFile

# A forced restart must bypass both the unchanged-input skip and pause for this
# explicit invocation, while leaving the scope paused afterward.
> reloadPause
$ delete target/server-started.txt
> reloadRestart
> waitForFile
> assertCanonicalJobIdentity
> reloadStatus
> reloadResume
